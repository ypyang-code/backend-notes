# 01. 两数之和

**日期**：2026-06-30  
**难度**：简单  
**链接**：[LeetCode 1](https://leetcode.cn/problems/two-sum)

## 题目描述

给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。

## 解题思路

使用暴力枚举，两层循环遍历数组，找到两数之和等于 target 的下标组合。

## 代码实现

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        int n = nums.length;
        for(int i=0;i<n-1;i++){
            for(int j = i + 1; j < n;j++){
                if(nums[i] + nums[j] == target){
                    return new int[]{i,j};
                }
            }    
        }
        return new int[]{};
    }
}

## 复杂度分析

- 时间复杂度：O(n²)
- 空间复杂度：O(1)

## 优化思路（待学习）

使用 HashMap 可以将时间复杂度优化到 O(n)。等学完 HashMap 源码后再来补充优化后的代码。

## 收获

完成了第一道 LeetCode，理解了暴力枚举的基本写法。下一题继续数组专题。
